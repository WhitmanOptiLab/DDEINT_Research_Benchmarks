<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="326" onload="init(evt)" viewBox="0 0 1200 326" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom(true);
			zoom(target);
			if (!document.querySelector('.parent')) {
				// we have basically done a clearzoom so clear the url
				var params = get_params();
				if (params.x) delete params.x;
				if (params.y) delete params.y;
				history.replaceState(null, null, parse_params(params));
				unzoombtn.classList.add("hide");
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		var sl = t.getSubStringLength(0, txt.length);
		// check if only whitespace or if we can fit the entire string into width w
		if (/^ *$/.test(txt) || sl < w)
			return;

		// this isn't perfect, but gives a good starting point
		// and avoids calling getSubStringLength too often
		var start = Math.floor((w/sl) * txt.length);
		for (var x = start; x > 0; x = x-2) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom(dont_update_text) {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			if(!dont_update_text) update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="326.0" fill="url(#background)"  />
<text id="title" x="600.00" y="24" >cv_model</text>
<text id="details" x="10.00" y="309" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="24" >Search</text>
<text id="ignorecase" x="1174.00" y="24" >ic</text>
<text id="matched" x="1090.00" y="309" > </text>
<g id="frames">
<g >
<title>dkl_4 (15,410,577 samples, 74.98%)</title><rect x="111.2" y="165" width="884.8" height="15.0" fill="rgb(226,88,88)" rx="2" ry="2" />
<text  x="114.23" y="175.5" >dkl_4</text>
</g>
<g >
<title>[ld-linux-x86-64.so.2] (501,822 samples, 2.44%)</title><rect x="11.2" y="229" width="28.8" height="15.0" fill="rgb(242,112,112)" rx="2" ry="2" />
<text  x="14.18" y="239.5" >[l..</text>
</g>
<g >
<title>all (20,553,612 samples, 100%)</title><rect x="10.0" y="277" width="1180.0" height="15.0" fill="rgb(209,63,63)" rx="2" ry="2" />
<text  x="13.00" y="287.5" ></text>
</g>
<g >
<title>[libgfortran.so.5.0.0] (830,317 samples, 4.04%)</title><rect x="63.6" y="229" width="47.6" height="15.0" fill="rgb(254,128,128)" rx="2" ry="2" />
<text  x="66.56" y="239.5" >[lib..</text>
</g>
<g >
<title>[unknown] (521,052 samples, 2.54%)</title><rect x="160.4" y="133" width="29.9" height="15.0" fill="rgb(205,58,58)" rx="2" ry="2" />
<text  x="163.41" y="143.5" >[u..</text>
</g>
<g >
<title>dde_drv3 (13,189,915 samples, 64.17%)</title><rect x="190.3" y="117" width="757.3" height="15.0" fill="rgb(228,92,92)" rx="2" ry="2" />
<text  x="193.32" y="127.5" >dde_drv3</text>
</g>
<g >
<title>dde_step (12,288,993 samples, 59.79%)</title><rect x="242.0" y="101" width="705.6" height="15.0" fill="rgb(235,101,101)" rx="2" ry="2" />
<text  x="245.04" y="111.5" >dde_step</text>
</g>
<g >
<title>[libc.so.6] (2,565,433 samples, 12.48%)</title><rect x="1042.7" y="181" width="147.3" height="15.0" fill="rgb(220,79,79)" rx="2" ry="2" />
<text  x="1045.72" y="191.5" >[libc.so.6]</text>
</g>
<g >
<title>dde_src2 (741,498 samples, 3.61%)</title><rect x="905.0" y="53" width="42.6" height="15.0" fill="rgb(209,63,63)" rx="2" ry="2" />
<text  x="907.99" y="63.5" >dde_..</text>
</g>
<g >
<title>__dde_solver_m_MOD_dde_zset2 (452,310 samples, 2.20%)</title><rect x="665.8" y="69" width="25.9" height="15.0" fill="rgb(209,63,63)" rx="2" ry="2" />
<text  x="668.78" y="79.5" >_..</text>
</g>
<g >
<title>[libgfortran.so.5.0.0] (830,317 samples, 4.04%)</title><rect x="63.6" y="213" width="47.6" height="15.0" fill="rgb(254,128,128)" rx="2" ry="2" />
<text  x="66.56" y="223.5" >[lib..</text>
</g>
<g >
<title>log_inline (894,207 samples, 4.35%)</title><rect x="319.4" y="53" width="51.3" height="15.0" fill="rgb(240,109,109)" rx="2" ry="2" />
<text  x="322.40" y="63.5" >log_i..</text>
</g>
<g >
<title>cv_model (20,553,612 samples, 100.00%)</title><rect x="10.0" y="261" width="1180.0" height="15.0" fill="rgb(228,91,91)" rx="2" ry="2" />
<text  x="13.00" y="271.5" >cv_model</text>
</g>
<g >
<title>[unknown] (104,875 samples, 0.51%)</title><rect x="34.0" y="197" width="6.0" height="15.0" fill="rgb(205,58,58)" rx="2" ry="2" />
<text  x="36.97" y="207.5" ></text>
</g>
<g >
<title>st_write_done (814,324 samples, 3.96%)</title><rect x="996.0" y="181" width="46.7" height="15.0" fill="rgb(230,94,94)" rx="2" ry="2" />
<text  x="998.97" y="191.5" >st_w..</text>
</g>
<g >
<title>[unknown] (104,875 samples, 0.51%)</title><rect x="34.0" y="213" width="6.0" height="15.0" fill="rgb(205,58,58)" rx="2" ry="2" />
<text  x="36.97" y="223.5" ></text>
</g>
<g >
<title>dde_err1 (900,922 samples, 4.38%)</title><rect x="190.3" y="101" width="51.7" height="15.0" fill="rgb(209,63,63)" rx="2" ry="2" />
<text  x="193.32" y="111.5" >dde_e..</text>
</g>
<g >
<title>[unknown] (19,620,651 samples, 95.46%)</title><rect x="63.6" y="245" width="1126.4" height="15.0" fill="rgb(205,58,58)" rx="2" ry="2" />
<text  x="66.56" y="255.5" >[unknown]</text>
</g>
<g >
<title>finalize_transfer (814,324 samples, 3.96%)</title><rect x="996.0" y="149" width="46.7" height="15.0" fill="rgb(244,114,114)" rx="2" ry="2" />
<text  x="998.97" y="159.5" >fina..</text>
</g>
<g >
<title>[libgfortran.so.5.0.0] (814,324 samples, 3.96%)</title><rect x="996.0" y="133" width="46.7" height="15.0" fill="rgb(254,128,128)" rx="2" ry="2" />
<text  x="998.97" y="143.5" >[lib..</text>
</g>
<g >
<title>st_write_done_worker (814,324 samples, 3.96%)</title><rect x="996.0" y="165" width="46.7" height="15.0" fill="rgb(212,68,68)" rx="2" ry="2" />
<text  x="998.97" y="175.5" >st_w..</text>
</g>
<g >
<title>[libgfortran.so.5.0.0] (410,507 samples, 2.00%)</title><rect x="40.0" y="245" width="23.6" height="15.0" fill="rgb(254,128,128)" rx="2" ry="2" />
<text  x="42.99" y="255.5" >[..</text>
</g>
<g >
<title>dde_wset (883,578 samples, 4.30%)</title><rect x="854.3" y="37" width="50.7" height="15.0" fill="rgb(240,109,109)" rx="2" ry="2" />
<text  x="857.27" y="47.5" >dde_w..</text>
</g>
<g >
<title>cv_model (15,410,577 samples, 74.98%)</title><rect x="111.2" y="181" width="884.8" height="15.0" fill="rgb(228,91,91)" rx="2" ry="2" />
<text  x="114.23" y="191.5" >cv_model</text>
</g>
<g >
<title>[libc.so.6] (16,224,901 samples, 78.94%)</title><rect x="111.2" y="213" width="931.5" height="15.0" fill="rgb(220,79,79)" rx="2" ry="2" />
<text  x="114.23" y="223.5" >[libc.so.6]</text>
</g>
<g >
<title>dde_zset (7,487,713 samples, 36.43%)</title><rect x="517.7" y="85" width="429.9" height="15.0" fill="rgb(219,78,78)" rx="2" ry="2" />
<text  x="520.69" y="95.5" >dde_zset</text>
</g>
<g >
<title>[ld-linux-x86-64.so.2] (522,454 samples, 2.54%)</title><rect x="10.0" y="245" width="30.0" height="15.0" fill="rgb(242,112,112)" rx="2" ry="2" />
<text  x="13.00" y="255.5" >[l..</text>
</g>
<g >
<title>ddes (2,559,691 samples, 12.45%)</title><rect x="370.7" y="69" width="147.0" height="15.0" fill="rgb(223,83,83)" rx="2" ry="2" />
<text  x="373.73" y="79.5" >ddes</text>
</g>
<g >
<title>dde_poly (2,844,446 samples, 13.84%)</title><rect x="741.7" y="53" width="163.3" height="15.0" fill="rgb(233,98,98)" rx="2" ry="2" />
<text  x="744.69" y="63.5" >dde_poly</text>
</g>
<g >
<title>dde_derv (4,159,158 samples, 20.24%)</title><rect x="278.9" y="85" width="238.8" height="15.0" fill="rgb(203,55,55)" rx="2" ry="2" />
<text  x="281.91" y="95.5" >dde_derv</text>
</g>
<g >
<title>dde_drv2 (14,032,976 samples, 68.27%)</title><rect x="190.3" y="133" width="805.7" height="15.0" fill="rgb(236,102,102)" rx="2" ry="2" />
<text  x="193.32" y="143.5" >dde_drv2</text>
</g>
<g >
<title>dde_zset2 (4,455,880 samples, 21.68%)</title><rect x="691.7" y="69" width="255.9" height="15.0" fill="rgb(249,121,121)" rx="2" ry="2" />
<text  x="694.75" y="79.5" >dde_zset2</text>
</g>
<g >
<title>[unknown] (2,565,433 samples, 12.48%)</title><rect x="1042.7" y="213" width="147.3" height="15.0" fill="rgb(205,58,58)" rx="2" ry="2" />
<text  x="1045.72" y="223.5" >[unknown]</text>
</g>
<g >
<title>dde_drv1 (15,410,577 samples, 74.98%)</title><rect x="111.2" y="149" width="884.8" height="15.0" fill="rgb(243,112,112)" rx="2" ry="2" />
<text  x="114.23" y="159.5" >dde_drv1</text>
</g>
<g >
<title>[unknown] (18,790,334 samples, 91.42%)</title><rect x="111.2" y="229" width="1078.8" height="15.0" fill="rgb(205,58,58)" rx="2" ry="2" />
<text  x="114.23" y="239.5" >[unknown]</text>
</g>
<g >
<title>__pow (1,599,467 samples, 7.78%)</title><rect x="278.9" y="69" width="91.8" height="15.0" fill="rgb(224,85,85)" rx="2" ry="2" />
<text  x="281.91" y="79.5" >__pow</text>
</g>
<g >
<title>[libc.so.6] (2,565,433 samples, 12.48%)</title><rect x="1042.7" y="197" width="147.3" height="15.0" fill="rgb(220,79,79)" rx="2" ry="2" />
<text  x="1045.72" y="207.5" >[libc.so.6]</text>
</g>
<g >
<title>dde_hst4 (843,061 samples, 4.10%)</title><rect x="947.6" y="117" width="48.4" height="15.0" fill="rgb(200,50,50)" rx="2" ry="2" />
<text  x="950.56" y="127.5" >dde_..</text>
</g>
<g >
<title>[ld-linux-x86-64.so.2] (394,602 samples, 1.92%)</title><rect x="11.3" y="213" width="22.7" height="15.0" fill="rgb(242,112,112)" rx="2" ry="2" />
<text  x="14.32" y="223.5" >[..</text>
</g>
<g >
<title>[ld-linux-x86-64.so.2] (394,602 samples, 1.92%)</title><rect x="11.3" y="181" width="22.7" height="15.0" fill="rgb(242,112,112)" rx="2" ry="2" />
<text  x="14.32" y="191.5" >[..</text>
</g>
<g >
<title>exp_inline (705,260 samples, 3.43%)</title><rect x="278.9" y="53" width="40.5" height="15.0" fill="rgb(217,75,75)" rx="2" ry="2" />
<text  x="281.91" y="63.5" >exp..</text>
</g>
<g >
<title>[ld-linux-x86-64.so.2] (394,602 samples, 1.92%)</title><rect x="11.3" y="197" width="22.7" height="15.0" fill="rgb(242,112,112)" rx="2" ry="2" />
<text  x="14.32" y="207.5" >[..</text>
</g>
<g >
<title>main (16,224,901 samples, 78.94%)</title><rect x="111.2" y="197" width="931.5" height="15.0" fill="rgb(242,112,112)" rx="2" ry="2" />
<text  x="114.23" y="207.5" >main</text>
</g>
</g>
</svg>
